name = "punbot"
main = "src/index.js"
compatibility_date = "2026-09-01"

[vars]
GITHUB_REPO = "proxymoron-byte/puntimes"   # owner/repo
GITHUB_BRANCH = "main"
SLACK_CHANNEL_ID = "C0C3M18RSH3"                 # the #puntimes channel (right-click channel > Copy link; ID is the last part)
SLACK_USER_ID = "U096RM2V7FC"                    # you (Slack profile > ⋮ > Copy member ID)
SITE_URL = "https://proxymoron-byte.github.io/puntimes/"
TIMEZONE = "Europe/Madrid"

# Secrets, set once with:
#   npx wrangler secret put SLACK_SIGNING_SECRET
#   npx wrangler secret put SLACK_BOT_TOKEN
#   npx wrangler secret put GITHUB_TOKEN
